import { StyleSheet, View, Image, Text } from 'react-native-web';

const ProfileScreen = () => {
  <View styles={styles.box}> ProfileScreen </View>;
};

const styles = StyleSheet.create({
  box: {
    fontSize: 45,
  },
});

export default ProfileScreen;
